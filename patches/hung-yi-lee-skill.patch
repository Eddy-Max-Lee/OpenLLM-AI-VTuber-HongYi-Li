diff --git a/scripts/hungyi_graph.py b/scripts/hungyi_graph.py
index 77f7031..2d51023 100644
--- a/scripts/hungyi_graph.py
+++ b/scripts/hungyi_graph.py
@@ -17,7 +17,7 @@ import json
 import math
 import re
 from collections import Counter, defaultdict
-from datetime import UTC, datetime
+from datetime import datetime, timezone
 from pathlib import Path
 from typing import Any
 
@@ -693,7 +693,7 @@ def generate_report(
     for _, _, data in G.edges(data=True):
         confidence_counts[data.get("confidence", "UNKNOWN")] += 1
 
-    now = datetime.now(UTC).replace(microsecond=0).isoformat()
+    now = datetime.now(timezone.utc).replace(microsecond=0).isoformat()
     lines = [
         "# Knowledge Graph Report",
         "",
diff --git a/scripts/hungyi_kb.py b/scripts/hungyi_kb.py
index 0190849..efe5788 100644
--- a/scripts/hungyi_kb.py
+++ b/scripts/hungyi_kb.py
@@ -19,7 +19,7 @@ import re
 import subprocess
 import sys
 from collections import Counter, defaultdict
-from datetime import UTC, datetime
+from datetime import datetime, timezone
 from pathlib import Path
 from typing import Iterable
 
@@ -176,7 +176,7 @@ def ensure_dirs() -> None:
 
 
 def now_iso() -> str:
-    return datetime.now(UTC).replace(microsecond=0).isoformat()
+    return datetime.now(timezone.utc).replace(microsecond=0).isoformat()
 
 
 def slugify(text: str) -> str:
@@ -197,12 +197,12 @@ def timestamp(seconds: float) -> str:
 def load_json(path: Path, default):
     if not path.exists():
         return default
-    return json.loads(path.read_text())
+    return json.loads(path.read_text(encoding="utf-8"))
 
 
 def save_json(path: Path, data) -> None:
     path.parent.mkdir(parents=True, exist_ok=True)
-    path.write_text(json.dumps(data, ensure_ascii=False, indent=2))
+    path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
 
 
 def run_yt_dlp_json(url: str, flat: bool = False) -> dict:
@@ -259,7 +259,7 @@ def transcript_path(video_id: str) -> Path:
 def parse_transcript_lines(path: Path) -> list[dict]:
     rows = []
     pattern = re.compile(r"^- \[(?P<stamp>\d{2}:\d{2}:\d{2})\] (?P<text>.*)$")
-    for line in path.read_text().splitlines():
+    for line in path.read_text(encoding="utf-8").splitlines():
         match = pattern.match(line)
         if not match:
             continue
@@ -400,7 +400,7 @@ def write_transcript_markdown(video: dict, rows: list[dict], language: str | Non
         if not text:
             continue
         body_lines.append(f"- [{timestamp(row['start'])}] {text}")
-    path.write_text("\n".join(body_lines) + "\n")
+    path.write_text("\n".join(body_lines) + "\n", encoding="utf-8")
     return path
 
 
@@ -490,7 +490,7 @@ def transcript_stats() -> dict:
     total_lines = 0
     markers = Counter()
     for path in sorted(TRANSCRIPTS_DIR.glob("*.md")):
-        text = path.read_text()
+        text = path.read_text(encoding="utf-8")
         total_lines += len(text.splitlines())
         for marker in STYLE_MARKERS:
             markers[marker] += text.count(marker)
@@ -510,7 +510,7 @@ def transcript_stats() -> dict:
 
 def write_markdown(path: Path, text: str) -> None:
     path.parent.mkdir(parents=True, exist_ok=True)
-    path.write_text(text.rstrip() + "\n")
+    path.write_text(text.rstrip() + "\n", encoding="utf-8")
 
 
 def append_log_entry(kind: str, title: str, details: list[str]) -> None:
@@ -801,7 +801,7 @@ def tokenize_query(query: str) -> list[str]:
 
 def transcript_snippets(path: Path, tokens: list[str], snippet_limit: int) -> list[str]:
     matches = []
-    for line in path.read_text().splitlines():
+    for line in path.read_text(encoding="utf-8").splitlines():
         if not line.startswith("- ["):
             continue
         lowered = line.lower()
@@ -954,7 +954,7 @@ def lint_wiki() -> None:
     if missing_topic_assignments:
         problems.append(f"{len(missing_topic_assignments)} videos missing topic assignments")
 
-    index_text = (WIKI_DIR / "index.md").read_text() if (WIKI_DIR / "index.md").exists() else ""
+    index_text = (WIKI_DIR / "index.md").read_text(encoding="utf-8") if (WIKI_DIR / "index.md").exists() else ""
     orphan_queries = []
     for path in QUERY_NOTES_DIR.glob("*.md"):
         if path.name not in index_text:
@@ -1044,7 +1044,7 @@ def run_graph_query(query: str) -> None:
         return
 
     data = json.loads(graph_json.read_text(encoding="utf-8"))
-    G = nx_json.node_link_graph(data, edges="links")
+    G = nx_json.node_link_graph(data)
     partition = {n: d.get("community", 0) for n, d in G.nodes(data=True)}
 
     result = query_graph(G, partition, query)
@@ -1085,7 +1085,7 @@ def run_graph_report() -> None:
         return
 
     data = json.loads(graph_json.read_text(encoding="utf-8"))
-    G = nx_json.node_link_graph(data, edges="links")
+    G = nx_json.node_link_graph(data)
     partition = {n: d.get("community", 0) for n, d in G.nodes(data=True)}
     community_labels = label_communities(G, partition)
 
